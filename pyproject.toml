[project]
name = "expense-tracker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "pandas",
    "plotly",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.52",
]
